name: DeveloperPortal Upload
run-name: DeveloperPortal Upload von  ${{ github.event.inputs.component }}:${{ github.event.inputs.componentVersion }} für ${{ github.event.inputs.environment }}

on:
  workflow_dispatch:
    inputs:
      component:
        description: Der Name der Komponente
        required: true
      componentVersion:
        description: Die Version der Komponente
        required: true
      tagPrefix:
        description: Der Präfix des Tags
        required: false
      environment:
        description: Die Zielumgebung
        required: true
      actionsRepositoryBranch:
        description: Zu benutzender Branch des Repositories mcbscore-github-actions
        required: false
        default: main
      deploymentRepositoryBranch:
        description: Welcher Branch von Repository ms-deployment benutzt werden soll
        required: false
        default: main

jobs:
  developerPortalUpload:
    # self-hosted Runner für die Steps benutzen, um Zugriff auf MD Systeme zu haben
    # TODO: ubuntu-latest benutzen? ms-deployment und Action sind in Github
    runs-on: [self-hosted, k8s]
    strategy:
      matrix:
        type: [open-api, system-api]
        version: [v1, v2]
    env:
      ACTIONS_REPOSITORY: freenet-group/mcbscore-github-actions
      ACTIONS_PATH: mcbscore/github
      DEPLOYMENT_PATH: mcbscore/deployment
      NODEJS_VERSION: 12
      PORTAL_URL: 'https://developer-portal-api.prod.developers.md.de/v2/specifications'

    steps:
      # Owner für alle Dateien des $GITHUB_WORKSPACE auf den Runner User setzen
      - name: Change Owner to Runner User
        if: ${{ always() }}
        run: |
          docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GH_R_PACKAGES }}
          docker run --rm -v `pwd`:/target -w /target -t ghcr.io/freenet-group/container-base-images/ubuntu_2004_lts:latest sh -c "chown $(id -u):$(id -g) -R /target"

      # Leeren Workspace sicherstellen
      - name: Ensure clean workspace
        run: |
          echo "Cleaning up previous run"
          rm -rf *

      # Repository auschecken unter $GITHUB_WORKSPACE
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read AWS Secrets
        uses: ./.github/actions/read-aws-secrets
        with:
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          awsParameterPairs: |
            /github/secrets/mcbs_token = TOKEN, 
            /github/secrets/mcbs_app.client_id_sts = CLIENT_ID_STS,
            /github/secrets/mcbs_app.client_secret_sts = CLIENT_SECRET_STS,
            /github/secrets/authorization_uri = AUTHORIZATION_URI

      # Auschecken des Deployment Repositories
      - name: Checkout Deployment Data / Scripts
        uses: actions/checkout@v4
        with:
          repository: freenet-group/ms-deployment
          token: ${{ env.TOKEN }}
          path: ${{ env.DEPLOYMENT_PATH }}
          ref: ${{ github.event.inputs.deploymentRepositoryBranch }}

      # Die internen Actions auschecken
      - name: checkout mcbscore-github-actions
        uses: actions/checkout@v4
        with:
          repository: ${{ env.ACTIONS_REPOSITORY }}
          token: ${{ env.TOKEN }}
          path: ${{ env.ACTIONS_PATH }}
          ref: ${{ github.event.inputs.actionsRepositoryBranch }}

      # ##################################################################################
      # Initialize Workflow
      # ##################################################################################
      - name: Initialize Workflow
        uses: ./mcbscore/github/actions/init-workflow
        with:
          installJavaWithNashornEngine: true
          installJq: true
          installNodeJs: true

      # Open API Yaml Dateien aus Release extrahieren
      - name: Download API documentation
        uses: ./mcbscore/github/actions/download-release-assets
        with:
          version: ${{ github.event.inputs.tagPrefix }}${{ github.event.inputs.componentVersion }}
          assetPattern: '.yaml'
          targetPath: './apiDoc'
          token: ${{ env.TOKEN }}

      # Developer Portal Upload
      # - name: Developer Portal Upload
      #   uses: ./mcbscore/github/actions/developer-portal-upload
      #   with:
      #     component: ${{ github.event.inputs.component }}
      #     componentVersion: ${{ github.event.inputs.componentVersion }}
      #     stage: ${{ github.event.inputs.environment }}
      #     tagPrefix: ${{ github.event.inputs.tagPrefix }}
      #     componentConfigPath: ${{ env.DEPLOYMENT_PATH }}

    - name: get API-ID
      run: |
       # TODO: API-ID auslesen
       echo "API_ID=someApiId" >> "$GITHUB_ENV"       
        
    - name: Checkout Github Action
      uses: actions/checkout@v4
      with:
          # For accessing to this Action a service account with a personal access token in this repo is necessary.
          token: ${{ env.TOKEN }}
          repository: freenet-group/developer-portal-actions
          ref: refs/heads/main
          persistent-credentials: false
          path: ./.github/actions/developer-portal-actions

    # Client-API ins Portal hochladen
    - name: Publish API Spec (client/open-api)
        if: ${{ matrix.type }} == 'open-api' && hashFiles('.apiDoc/${{ github.event.inputs.component }}_client_${{ matrix.version }}.yml') != ''
      uses: ./.github/actions/developer-portal-actions/publish-api-spec
      with:
          portalUrl: ${{ env.PORTAL_URL }}
          documentPath: .apiDoc/${{ github.event.inputs.component }}_client_${{ matrix.version }}.yml
          stage: ${{ github.event.inputs.environment }}
          maturityLevel:  ${{ matrix.type }}
          apiId: ${{ env.API_ID }}
          version: ${{ github.event.inputs.componentVersion }}
          clientId: ${{ env.CLIENT_ID_STS }}
          clientSecret: ${{ env.CLIENT_SECRET_STS }}

    # System-API ins Portal hochladen
    - name: Publish API Spec (system/system-api)
        if: ${{ matrix.type }} == 'system-api' && hashFiles('.apiDoc/${{ github.event.inputs.component }}_system_${{ matrix.version }}.yml') != ''
      uses: ./.github/actions/developer-portal-actions/publish-api-spec
      with:
          portalUrl: ${{ env.PORTAL_URL }}
          documentPath: .apiDoc/${{ github.event.inputs.component }}_system_${{ matrix.version }}.yml
          stage: ${{ github.event.inputs.environment }}
          maturityLevel:  ${{ matrix.type }}
          apiId: ${{ env.API_ID }}
          version: ${{ github.event.inputs.componentVersion }}
          clientId: ${{ env.CLIENT_ID_STS }}
          clientSecret: ${{ env.CLIENT_SECRET_STS }}