name: Distribute
run-name: Distribute für "${{ github.event.inputs.group }}"

on:
  schedule:
    # jeden Sonntag, 05:00h
    - cron: "0 5 * * 6"
  workflow_dispatch:
    inputs:
      group:
        description: 'Die zu aktualisierende Gruppe. Per default werden "ALL (alle Workflows)" aktualisiert'
        type: choice
        required: true
        default: ALL
        options:
          - ALL
          - aws
          - errorcodes
          - framework
          - mandel
          - mcbs
          - metis
          - ms
          - ms-cicd
          - mslib
          - npm-lib
          - proxy
          - rating
          - simple
          - standard
          - test
          - spotless

jobs:
  aws:
    if: ${{ github.event.inputs.group == 'aws' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: aws-ms-events, branch: develop, group: aws, workflows: "build, deployment, deployment_dev, deployment_git, deployment_playground, deployment_prod, remove, remove_dev, remove_git, remove_playground, remove_prod" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  errorcodes:
    if: ${{ github.event.inputs.group == 'errorcodes' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: md-error-codes, branch: main, group: errorcodes, workflows: "../check, build" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  mandel:
    if: ${{ github.event.inputs.group == 'mandel' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbscore-mandel, branch: develop, group: mandel, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  mcbs:
    if: ${{ github.event.inputs.group == 'mcbs' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbs-master, branch: develop, group: mcbs, workflows: "../check, preCheck, build, dbChanges, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild_MCBS, postBuild_MS, sonarqube, MCBS-Freigabe, refcalls_deployment, setPRIssueDone" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  metis:
    if: ${{ github.event.inputs.group == 'metis' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: metis-test, branch: develop, group: metis, workflows: "../check, test/build, ../encoding" }
          - { name: metis-ui, branch: develop, group: metis, workflows: "../check, ui/build, ui/deployment, ui/deployment_dev, ui/deployment_test, ui/deployment_prod, ui/deployment_k8s, ../encoding" }
          - { name: metis, branch: develop, group: metis, workflows: "../check, ../encoding, ../vulnerability-detection, backend/build, backend/postBuild, backend/dbChanges, backend/deployment, ../ms/deployment_k8s_only, backend/rest-deployment, backend/database-init" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  ms-cicd:
    if: ${{ github.event.inputs.group == 'ms-cicd' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    env:
      DEFAULT_WORKFLOWS: "../encoding, ../developerPortal, ../developer-portal-internal, ../vulnerability-detection, build, check_code, check_pull_request, postBuild, release, deployment"
    strategy:
      matrix:
        # Bei Bedarf kann als "workflows" eine abweichende Liste von zu verteilenden Workflow-Dateien angegeben werden
        repository:
          - { name: ms-admin-dashboard, branch: main, group: ms-cicd }
          - { name: ms-alteration, branch: main, group: ms-cicd }
          - { name: ms-appcheck, branch: main, group: ms-cicd }
          - { name: ms-authorities-information, branch: main, group: ms-cicd }
          - { name: ms-billingaccount-frontend, branch: main, group: ms-cicd }
          - { name: ms-commondata, branch: main, group: ms-cicd }
          - { name: ms-configserver, branch: main, group: ms-cicd }
          - { name: ms-contentprovider, branch: main, group: ms-cicd }
          - { name: ms-customerchange, branch: main, group: ms-cicd, workflows: "../encoding, ../developerPortal, ../developer-portal-internal, ../vulnerability-detection, build, check_code, check_pull_request, postBuild, dbChanges, release, deployment" }
          - { name: ms-discovery, branch: main, group: ms-cicd }
          - { name: ms-elasticupdate, branch: main, group: ms-cicd }
          - { name: ms-find, branch: main, group: ms-cicd }
          - { name: ms-homer, branch: main, group: ms-cicd }
          - { name: ms-invoice, branch: main, group: ms-cicd }
          - { name: ms-mark, branch: main, group: ms-cicd }
          - { name: ms-rating, branch: main, group: ms-cicd }
          - { name: ms-user-cleanup, branch: main, group: ms-cicd, workflows: "../encoding, ../vulnerability-detection, build, check_code, check_pull_request, postBuild, release, deployment" }
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Workflows verteilen
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows || env.DEFAULT_WORKFLOWS  }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  ms:
    if: ${{ github.event.inputs.group == 'ms' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: ms-agreement, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, postBuild, setPRIssueDone" }
          - { name: ms-businessinteraction-router, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-businesstransaction, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-customerproduct, branch: develop, group: ms, workflows: "../check, build, ../encoding" }
          - { name: ms-delegate, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-event-relay, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-freeprint-router, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-gateway, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-note-router, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, postBuild, ../vulnerability-detection, setPRIssueDone" }
          - { name: ms-oidc, branch: develop, group: ms, workflows: "../check, build, dbChanges, deployment, ../developerPortal, ../developer-portal-internal, postBuild, setPRIssueDone" }
          - { name: ms-oms-router, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, postBuild, ../vulnerability-detection, setPRIssueDone" }
          - { name: ms-proxy-support, branch: develop, group: ms, workflows: "../check, build, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild, setPRIssueDone" }
          - { name: ms-user, branch: develop, group: ms, workflows: "../check, build, dbChanges, deployment, ../developerPortal, ../developer-portal-internal, ../encoding, ../vulnerability-detection, postBuild, setPRIssueDone" }

          # ms-deployment/deployment_k8s: Kubernetes Namespace Konfig
          - { name: ms-deployment, branch: main, group: ms-deployment, workflows: "deployment_k8s" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  mslib:
    if: ${{ github.event.inputs.group == 'mslib' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mslib-aws, branch: develop, group: mslib, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mslib-rest, branch: develop, group: mslib, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  npm-lib:
    if: ${{ github.event.inputs.group == 'npm-lib' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    env:
      DEFAULT_WORKFLOWS: "deployment, pullRequestCheck, pullRequestTests, release"
    strategy:
      matrix:
        # Bei Bedarf kann als "workflows" eine abweichende Liste von zu verteilenden Workflow-Dateien angegeben werden
        repository:
          - { name: async-api-event-generator, branch: main, group: npm-lib }
          - { name: eslint-plugin-mcbs, branch: main, group: npm-lib } 
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Workflows verteilen
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows || env.DEFAULT_WORKFLOWS  }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  proxy:
    if: ${{ github.event.inputs.group == 'proxy' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbscore-authorities-information-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-appcheck-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-billingaccount-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-businesstransaction-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-commondata-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-contentprovider-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-cuba-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-customerchange-proxy, branch: develop, group: proxy, workflows: "../check, deployment, build, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-customerproduct-proxy, branch: develop, group: proxy, workflows: "../check, deployment, build, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-customer-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-elasticupdate-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-find-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-healthcheck-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_md, deployment_dev, deployment_dev_md, deployment_git, deployment_git_md, deployment_prod, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-invoice-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-mark-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-oidc-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_fm, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_fm, deployment_git_fm_extern, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_fm, deployment_prod_fm_extern, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-rating-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-user-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_km_extern, deployment_git_md, deployment_git_md_extern, deployment_prod, deployment_prod_km, deployment_prod_km_extern, deployment_prod_md, deployment_prod_md_extern, ../encoding, postBuild" }
          - { name: mcbscore-zap-businesstransaction-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-contract-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-fee-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-invoice-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-mark-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-network-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-zap-party-service-proxy, branch: develop, group: proxy, workflows: "../check, build, deployment, deployment_b2b, deployment_b2b_km, deployment_b2b_md, deployment_b2b_km_regtestalt, deployment_b2b_km_regtestneu, deployment_b2b_md_regtestalt, deployment_b2b_md_regtestneu, deployment_b2b_km_b2balt, deployment_b2b_km_b2bneu, deployment_b2b_km_ngbilling, deployment_b2b_md_b2balt, deployment_b2b_md_b2bneu, deployment_b2b_md_ngbilling, deployment_dev, deployment_dev_km, deployment_dev_md, deployment_git, deployment_git_km, deployment_git_md, deployment_prod, deployment_prod_km, deployment_prod_md, ../encoding, postBuild" }
          - { name: mcbscore-lua, branch: develop, group: proxy, workflows: "../check, build_lua, deployment_lua, deployment_lua_b2b, deployment_lua_dev, deployment_lua_git, deployment_lua_prod, ../encoding, postBuild_lua" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  rating:
    if: ${{ github.event.inputs.group == 'rating' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbscore-rating, branch: develop, group: rating, workflows: "../check, build, dbChanges, ../encoding, ../vulnerability-detection, postBuild, Git-Deployment" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  simple:
    if: ${{ github.event.inputs.group == 'simple' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbs-logging, branch: develop, group: simple, workflows: "../check, ../vulnerability-detection, build" }
          - { name: mcbscore-billing-performance-monitor, branch: main, group: simple, workflows: "../encoding" }
          - { name: mcbscore-yaml-transform-plugin, branch: develop, group: simple, workflows: "../check, ../encoding, ../vulnerability-detection, build" }
          - { name: mcbscore-reusable-workflows, branch: main, group: simple, workflows: "../ms/deployment_k8s"}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  standard:
    if: ${{ github.event.inputs.group == 'standard' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbscore-bot, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mcbscore-changelog-editor, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mcbscore-customer-search-index, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mcbscore-db-replica, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mcbscore-es-plugin-collection, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: mcbscore-framework, branch: develop, group: standard, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  framework:
    if: ${{ github.event.inputs.group == 'framework' || github.event.inputs.group == 'ALL' || github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: lib-amount, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-annotation, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-common, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-enterprise, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-io, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-persistence, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-process, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-relation, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-security, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
          - { name: lib-service, branch: develop, group: framework, workflows: "../check, build, ../encoding, ../vulnerability-detection, postBuild" }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  test:
    # nur bei Input "test". Auch bei "ALL" wäre üblicherweise redundant, weil hier eine Teilmenge der obigen Repos konfiguriert wird.
    if: ${{ github.event.inputs.group == 'test' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repository:
          - { name: mcbscore-reusable-workflows, branch: main, group: simple, workflows: "../ms/deployment_k8s"}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Workflows
        uses: ./.github/actions/distribute-workflows
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryGroup: ${{ matrix.repository.group }}
          repositoryBranch: ${{ matrix.repository.branch }}
          workflows: ${{ matrix.repository.workflows }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  spotless:
    if: ${{ github.event.inputs.group == 'spotless' }}
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        repository:
          - { name: lib-amount, branch: develop }
          - { name: lib-annotation, branch: develop }
          - { name: lib-common, branch: develop }
          - { name: lib-enterprise, branch: develop }
          - { name: lib-io, branch: develop }
          - { name: lib-persistence, branch: develop }
          - { name: lib-process, branch: develop }
          - { name: lib-relation, branch: develop }
          - { name: lib-security, branch: develop }
          - { name: lib-service, branch: develop }
          - { name: mcbs-master, branch: develop }
          - { name: mcbs-logging, branch: develop }
          - { name: mcbscore-mandel, branch: develop }
          - { name: mcbscore-bot, branch: develop }
          - { name: mcbscore-changelog-editor, branch: develop }
          - { name: mcbscore-customer-search-index, branch: develop }
          - { name: mcbscore-db-replica, branch: develop }
          - { name: mcbscore-es-plugin-collection }
          - { name: mcbscore-framework, branch: develop }
          - { name: mcbscore-yaml-transform-plugin, branch: develop }
          - { name: metis, branch: develop }
          - { name: ms-alteration, branch: main }
          - { name: ms-commondata, branch: main }
          - { name: ms-admin-dashboard, branch: main }
          - { name: ms-agreement, branch: develop }
          - { name: ms-appcheck, branch: main }
          - { name: ms-authorities-information, branch: main }
          - { name: ms-billingaccount-frontend, branch: main }
          - { name: ms-businessinteraction-router, branch: develop }
          - { name: ms-businesstransaction, branch: develop }
          - { name: ms-configserver, branch: main }
          - { name: ms-contentprovider, branch: main }
          - { name: ms-customerchange, branch: main }
          - { name: ms-customerproduct, branch: develop }
          - { name: ms-delegate, branch: develop }
          - { name: ms-discovery, branch: main }
          - { name: ms-elasticupdate, branch: main }
          - { name: ms-event-relay, branch: develop }
          - { name: ms-find, branch: develop }
          - { name: ms-freeprint-router, branch: develop }
          - { name: ms-gateway, branch: develop }
          - { name: ms-homer, branch: main }
          - { name: ms-invoice, branch: develop }
          - { name: ms-mark, branch: main }
          - { name: ms-note-router, branch: develop }
          - { name: ms-oidc, branch: develop }
          - { name: ms-oms-router, branch: develop }
          - { name: ms-proxy-support, branch: develop }
          - { name: ms-rating, branch: main }
          - { name: ms-user, branch: develop }
          - { name: ms-user-cleanup, branch: main }
          - { name: mslib-aws, branch: develop }
          - { name: mslib-rest, branch: develop }
          - { name: scaffold-clean-architecture-gradle-plugin, branch: develop }

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Distribute Spotless
        uses: ./.github/actions/distribute-spotless
        with:
          repositoryName: ${{ matrix.repository.name }}
          repositoryBranch: ${{ matrix.repository.branch }}
          awsAccessKeyId: ${{ secrets.AWS_ACCESS_KEY_ID }}
          awsSecretAccessKey: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          artifactoryUserName: ${{ secrets.MD_ARTIFACTORY_USER }}
          artifactoryPassword: ${{ secrets.MD_ARTIFACTORY_PW }}
